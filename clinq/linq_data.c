#include <stdio.h>

#include "linq_data.h"


COLLECTION	*linq_concat(COLLECTION *src, COLLECTION *second) { return NULL; }
COLLECTION	*linq_default_if_empty(COLLECTION *src, void *default_value) { return NULL; }
COLLECTION	*linq_reverse(COLLECTION *src) { return NULL; }

COLLECTION	*linq_skip(COLLECTION *src, int count) { return NULL; }
COLLECTION	*linq_skip_while(COLLECTION *src, PREDICATE) { return NULL; }
COLLECTION	*linq_take(COLLECTION *src, int count) { return NULL; }
COLLECTION	*linq_take_while(COLLECTION *src, PREDICATE) { return NULL; }