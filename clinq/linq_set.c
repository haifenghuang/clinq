#include <stdio.h>

#include "linq_set.h"

COLLECTION *linq_distinct(COLLECTION *src, EQ_COMPARITOR) { return NULL; }
COLLECTION *linq_except(COLLECTION *src, EQ_COMPARITOR) { return NULL; }
COLLECTION *linq_intersect(COLLECTION *src, COLLECTION *second, EQ_COMPARITOR) { return NULL;  }
COLLECTION *linq_union(COLLECTION *src, COLLECTION *second, EQ_COMPARITOR) { return NULL; }